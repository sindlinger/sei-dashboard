#!/usr/bin/env bash
# Wrapper para facilitar chamada do CLI do projeto.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR" || exit 1
UV_LINK_MODE=copy uv run python -m cli "$@"
