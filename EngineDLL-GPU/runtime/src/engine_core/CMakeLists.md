# CMakeLists.txt (engine_core, runtime)

## Visão geral
- Script idêntico ao utilizado na árvore de desenvolvimento para compilar `GpuEngine.dll` (CUDA/cuFFT).
- Mantido apenas para garantir que o runtime tenha a mesma configuração caso seja necessário compilar localmente.
