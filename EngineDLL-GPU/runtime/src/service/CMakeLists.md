# CMakeLists.txt (service, runtime)

## Visão geral
- Script de build do `GpuEngineService.exe` copiado da árvore de desenvolvimento.
- Mantido para permitir recompilação direta no ambiente runtime; alterações devem ser feitas em `Dev/src/service/`.
