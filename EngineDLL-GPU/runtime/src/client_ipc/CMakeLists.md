# CMakeLists.txt (client_ipc, runtime)

## Visão geral
- Script de build reproduzido da árvore `Dev/src/client_ipc/`, definindo a compilação do `GpuEngineClient.dll`.

## Papel no runtime
- Mantido apenas para referência/diagnóstico; alterações efetivas devem ocorrer na versão de desenvolvimento e ser propagadas pelos scripts de sincronização.
