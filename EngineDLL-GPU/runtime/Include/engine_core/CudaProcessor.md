# CudaProcessor.h (runtime)

## Visão geral
- Header sincronizado com `Dev/Include/engine_core/CudaProcessor.h`; declara a classe responsável pelo pipeline CUDA do engine.

## Papel no runtime
- Fornecido para compilações runtime desempenhadas pelos scripts; alterações devem ocorrer na versão `Dev/` e ser replicadas.
