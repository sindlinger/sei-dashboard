# Service.h (runtime)

## Visão geral
- Declaração sincronizada da classe `Service` que equipa o executável `GpuEngineService.exe` distribuído no runtime.
- Mantém compatibilidade com o código-fonte de desenvolvimento; editar somente a versão `Dev/`.
